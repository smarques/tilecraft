{
  "name": "tilecraft",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler pages dev .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
